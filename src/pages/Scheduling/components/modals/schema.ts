// import { z } from "zod";

// export const alterSchema = (isEdit = false) => z.object({});

// export type TAlterScheduleSchema = z.infer<ReturnType<typeof alterSchema>>;

// export const AlterScheduleSchema = alterSchema();

// export const scheduleDefaultValues: Partial<TAlterScheduleSchema> = {};
